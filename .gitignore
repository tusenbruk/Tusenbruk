# build
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# source material — stays local, not in the deployed repo
# (anchored to the repo root so public/photos still gets committed)
/_archive/
/photos/
/content-source/

# os
.DS_Store

# env
.env
.env.local
